{
  "name": "vitrin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check js/store.js && node --check js/app.js && node --check scripts/build.mjs",
    "test": "npm run check && node tests/smoke.mjs"
  }
}
